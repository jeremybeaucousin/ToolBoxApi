package com.scalian.services

final object ApiConstants {
  final object Session {
    final val userConnectedKey = "connectedUser"
    
    final object UserKeys {
      final val login = "login"
      final val password = "password"
    }
  }
}